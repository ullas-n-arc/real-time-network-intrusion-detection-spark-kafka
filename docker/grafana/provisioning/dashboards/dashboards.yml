apiVersion: 1

providers:
  - name: 'NIDS Dashboards'
    orgId: 1
    folder: 'Network Intrusion Detection'
    folderUid: 'nids-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
